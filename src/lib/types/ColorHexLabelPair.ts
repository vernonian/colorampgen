/**
 * This type is an object with a pair of strings:
 * hex: A hex code
 * label: A label to go with hex code
 */

export type ColorHexLabelPair = {
  hex: string;
  label: string;
};

export type ColorHexLabelPair;